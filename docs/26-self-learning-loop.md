# 26 — Self-learning loop: реализация и последовательный тест

Минимальный рабочий цикл, в котором Claude сам формирует гипотезу из сырых
данных, принимает решения, разбирает исходы и **меняет собственное поведение**.
ACE.pine не тронут. Бирж, брокеров, ордеров, исполнения и уведомлений нет.

---

## 1. УСТРОЙСТВО

```
MARKET DATA → features.py      только бары ≤ i, никаких паттернов
DECISION    → policy.decide()  исполняемые правила, первое совпавшее решает
   ══ барьер ══ ledger.append('DECISION')  SHA256 + цепочка, ДО симуляции
OUTCOME     → engine.simulate()            только после печати решения
POSTMORTEM  → report.py        показывает ТОЛЬКО закрытые сделки
LESSON      → review.py        пишется в тот же леджер
BELIEF      → beliefs.py       статус пересчитывается арифметикой, не риторикой
BEHAVIOR    → policies/vN.json новая версия политики
VERIFY      → verify_change.py сколько решений реально разошлось
```

### Почему стратегия не задана заранее

`features.py` отдаёт только описательные примитивы: доходности за 5/20/60 баров,
диапазон бара к медиане, положение закрытия в баре и в 20/60-барном окне,
волатильность, серии однонаправленных баров, час UTC, ход от открытия дня.
**Нет** FVG, BOS, CHoCH, Turtle Soup, тренда и любых именованных сетапов.
Какие из примитивов важны и как — решает политика, а не библиотека признаков.

### Три механизма барьера

| Механизм | Что делает |
|---|---|
| Структурный | `features(b[:i+1], i)` получает **срез** — бар i+1 не «не читается», его нет |
| Криптографический | решение хэшируется в append-only леджер до симуляции; цепочка `prev→chain` ловит переписывание |
| Аудиторский | `tests/test_barrier.py` мутирует все бары после i и требует неизменности решения |

**С негативным контролем.** Заведомо протекающий путь (читает бар i+20) обязан
падать: **9 утечек из 60** против **0 из 60** у настоящего кода. Проверка,
которая никогда не падала, — не проверка (`docs/24`).

Механизм убийства гипотезы тоже протестирован отдельно (`tests/test_kill.py`):
на синтетической записи 20% побед статус обязан стать `REJECTED`. Поэтому
«гипотеза выжила» — это факт, а не молчание кода.

---

## 2. ДАННЫЕ

| Сегмент | Файл | Баров | Чистота |
|---|---|---|---|
| LEARN | `research/blank/DEV.csv` | 4 590 | использовался раньше; гипотеза выведена здесь |
| SEG-1 | `research/loop/TEST.csv` | 1 751 | **девственный** — `XAU-EXCLUDED-tail`, 09-09…09-14, не участвовал ни в одном тесте |
| SEG-2 | `research/blank/FORWARD.csv` | 8 760 | загрязнён частично: здесь шёл эксперимент «пустой график» с **другой** стратегией; известен дрейф +436 пунктов вверх — для шорт-политики это помеха, то есть смещение работает **против** гипотезы |

---

## 3. ГИПОТЕЗА H1 — ВЫВЕДЕНА ИЗ ДАННЫХ, НЕ ЗАДАНА

Посчитал на LEARN долю движений вверх за 20 баров при разных состояниях.
База по всем 4 410 точкам: **47.9% вверх**.

| Состояние | n | доля вверх |
|---|---|---|
| узкий бар `rng < 0.4` | 123 | **34.1%** |
| узкий бар `rng < 0.5` | 357 | **39.5%** |
| `span20 < 2` | 75 | **28.0%** |
| `span20 < 3` | 741 | **40.4%** |
| новый максимум 20 + закрытие вверху | 157 | 51.0% |
| 3+ бара подряд вверх | 473 | 46.9% |

Монотонно по обоим порогам, дрейф самого LEARN всего −33 пункта.

> **H1: после сжатия волатильности цена чаще идёт вниз.**
> Правило v1: `rng < 0.5 and span20 < 4` → SHORT.
> Kill criteria заданы до первой сделки.

Выходы: SL = TP = 0.15%, максимум 48 баров, кулдаун 20, при касании стопа и
цели на одном баре засчитывается стоп.

---

## 4. ХОД ЦИКЛА

### Партия 1 — 8 сделок, 4/8, 0.0R → политика НЕ изменена

Сравнил победы и убытки по всем признакам: ни `rng`, ни `span20`, ни `net20`,
ни `pos60`, ни час не разделяют. Записал в леджер решение **не менять**:
правило, выведенное из четырёх убытков, было бы подгонкой под шум — ровно тот
`POST_LOSS_FLIP`, который назван режимом отказа в `docs/23`.

Способность цикла **не** реагировать на шум — такая же часть обучения, как
способность реагировать.

### Партия 2 — 16 сделок, 8/8, 0.0R → УРОК L1, политика v2

Отсортировал сделки по `net20` (ход за 20 баров в медианных диапазонах):

```
  -2.68  STOP    -2.54  STOP    -2.18  STOP    -1.97  STOP
  -1.80  TARGET  -1.07  TARGET  -0.96  TARGET  ...
```

Четыре самых отрицательных — все четыре стопы. Ближайшая победа на −1.80.

> **L1: сжатие стоит шортить, только пока ход вниз не исчерпан.**
> Когда падение за 20 баров уже произошло, цели на те же 0.15% не остаётся.

Правило `R0: net20 <= -2.0 → HOLD`, вперёд R1.

**Антиподгонка:** порог взят круглым −2.0, а не по краю зазора −1.85. Это
отсекает 3 убытка из 4 вместо 4 — сознательная потеря ради того, чтобы не
подгонять под конкретный разрыв в выборке n=4.

### Нарушение протокола — найдено и записано

`verify_change.py` вызвал `free_replay` без ограничения горизонта и напечатал
агрегаты по **всему** TEST.csv, включая бары после 1324, которые я ещё не решал.
Увидел: `v1 n=19 +3.0R 11/19; v2 n=18 +8.0R 13/18` — только агрегаты, без
отдельных сделок, баров и признаков.

Записано в леджер как `BREACH`. Смягчение: R0 был зафиксирован **до** этого
просмотра, поэтому на само правило утечка повлиять не могла; `free_replay`
ограничен последним решённым баром; любое будущее изменение политики не вправе
ссылаться на этот агрегат.

### Партия 3 — сегмент исчерпан, 3 сделки, 3/3

---

## 5. РЕЗУЛЬТАТЫ

| | сделок | побед | итог | ожидание |
|---|---|---|---|---|
| SEG-1 (девственный) | 19 | 11 (58%) | +3.0R | +0.158R |
| SEG-2 (дрейф вверх) | 25 | 11 (44%) | −3.5R | −0.141R |
| **вместе** | **44** | **22 (50%)** | **−0.5R** | **−0.012R** |

Спред ≈ 0.06R на сделку. С издержками ожидание ≈ −0.07R. **Как и во всех
предыдущих фазах, издержки превышают измеренный эффект** — на 44 сделках
отличить это от нуля нельзя.

Kill criteria H1: `wr < 0.35` → нет (0.50); `ожидание хуже −0.25R` → нет
(−0.012R). **H1 выживает механически**, и это проверяемое утверждение, потому
что механизм убийства протестирован отдельно.

---

## 6. ГЛАВНАЯ ПРОВЕРКА: УРОК ИЗМЕНИЛ ПОВЕДЕНИЕ ИЛИ ОСТАЛСЯ ТЕКСТОМ?

Три разных измерения, и только одно из них выглядит убедительно.

| Измерение | Результат |
|---|---|
| **Контрфактуально на истории** (744 точки SEG-1 под v1 против v2) | **3 решения расходятся**, все три — реальные убыточные сделки (бары 528, 1113, 1297) |
| **Вперёд на SEG-1** после принятия правила (282 решения) | **0.** Правило не связало ни разу: сегмент кончился раньше |
| **Вперёд на SEG-2**, которого правило не видело (25 сделок) | **5 блокировок**, из них **4 убытка и 1 победа**; заблокированное в сумме дало бы **−3.0R** |

Оговорка к третьей строке: сравнение держит точки решения фиксированными;
блокировка сдвигает кулдауны, поэтому «без R0 было бы −6.5R вместо −3.5R» —
оценка, а не точный счёт.

Отдельно важно: R0 «сработал 101 раз» под v2 — **обманчивая цифра**, которую я
сначала и получил. На 96 из этих баров R1 всё равно не выстрелил бы: R0 блокировал
то, что и так было бы HOLD. Значимо только число решений, которые **разошлись**.

**Ответ: правило реально изменило поведение — но доказано это только на втором
сегменте, где n=5.** На сегменте, где урок был выучен, вперёд он не связал ни
разу. Это слабое подтверждение, а не сильное.

---

## 7. ЧТО ЭТОТ ТЕСТ ПОКАЗАЛ И ЧЕГО НЕ ПОКАЗАЛ

**Показал:** механизм работает. Гипотеза формируется из сырых данных; решение
физически фиксируется до исхода; убеждения пересчитываются арифметикой;
урок превращается в исполняемое правило; изменение поведения измеряется, а не
декларируется; цикл умеет **не** менять политику на шуме и умеет найти и
записать собственное нарушение протокола.

**Не показал:** что обучение даёт преимущество. 44 сделки, 50% побед,
−0.012R — это неотличимо от нуля, и издержки больше эффекта. Один урок,
подтверждённый на пяти прямых блокировках, — это начало доказательства, а не
доказательство.

**Чего не хватает для настоящего ответа** (как и в Phase 0): **адаптивного
механического компаратора** — правила, которое получает те же данные и ту же
свободу менять себя, но без участия Claude. Без него «Claude научился»
неотличимо от «любая адаптация на этих данных дала бы то же самое».
Это следующий шаг, и он не сделан.

---

# ЭТАП 2 — продолжение обучения

## 8. ПОЧЕМУ SEG-1 ДАЛ 58% И +3R

Первый ответ: **возможно, ни почему.** 11/19 неотличимо от монетки
(двусторонний биномиальный p = 0.65), разница SEG-1 минус SEG-2 = +0.299R
с перестановочным p = 0.193. Объяснять на 19 сделках нечего.

Структурный кандидат есть: SEG-1 упал на 38 пунктов, SEG-2 вырос на 436, а
политика умеет только шортить. Но дрейф известен лишь задним числом, поэтому
проверил причинные прокси (`net60`, `span60`) — ничего чистого. Правила под
эти 19 сделок не подгонял: выходы не трогал, часовых фильтров не добавлял,
по отдельности сделки SEG-1 не разбирал.

## 9. СЕГМЕНТЫ

| Сегмент | Период | Баров | Статус |
|---|---|---|---|
| LEARN | 07-12 → 07-24 | 4 590 | гипотеза выведена здесь |
| SEG-1 | 09-09 → 09-14 | 1 751 | девственный |
| SEG-2/3 | 07-26 → 08-20 | 8 760 | другая стратегия раньше; дрейф +436 |
| SEG-4 | 08-21 → 09-08 | 5 917 | **0 пересечений** со всем остальным |

**Пойманная ошибка.** Завёл SEG-4 из `XAU-CALIBRATION` как «другой период».
Сверка меток времени: `CALIBRATION == DEV + FORWARD` побитово, **0 уникальных
баров**. Шестьдесят «новых» сделок оказались повтором обучающей выборки.
Леджер отправлен в `DISCARDED-seg4-duplicate.jsonl` и в анализ не входит;
инцидент записан как `BREACH`. Поймал по совпадению ценового диапазона
4086→4526 — до печати результатов.

**Оговорка по хронологии.** SEG-1 (09-09→09-14) идёт **позже** SEG-4
(08-21→09-08), а урок L1 выведен из SEG-1. Значит правило, выученное на более
позднем периоде, применяется к более раннему. Внутри сегмента заглядывания
вперёд нет, но чистым forward-тестом это не является.

## 10. УРОК L2 И ЕГО СУДЬБА

**Что понял.** На 44 сделках порог исчерпанности хода оказался не −2.0, а
−1.0: `net20 <= -1.0` давало 3 победы из 12 (25%) против 19 из 32 (59%).
Подтверждалось раздельно: SEG-1 2/6, SEG-2 1/6 — причём SEG-2 правило не
порождал.

**Что изменил.** `R0: net20 <= -2.0` → `net20 <= -1.0`, политика v3 с бара 2742.

**Как это изменило решения.** Контрфактуально на истории — 6 расхождений, все
шесть реальные сделки (5 убытков, 1 победа). Вперёд на SEG-3 — **11 блокировок,
1 победа из 11, предотвращено −9.0R.** Сильное проспективное подтверждение.

**Результат новых решений.** Под v3 на SEG-3: 40 сделок, 17 побед (42%),
−6.0R. Фильтр убрал заведомо проигрышное подмножество, но остаток всё равно
не зарабатывал.

**А потом L2 сломался.** На SEG-4 тот же фильтр заблокировал **33 сделки,
которые дали бы +5.0R при 19 победах из 33**. Там он резал прибыль.

Проверка на уровне всех 180 кандидатов трёх непересекающихся сегментов:

| Сегмент | `net20<=-1` | `net20>-1` | разница средних |
|---|---|---|---|
| SEG-1 | n=6 wr 0.33 | n=13 wr 0.69 | −0.72R |
| SEG-2/3 | n=20 wr 0.35 | n=73 wr 0.49 | −0.29R |
| SEG-4 | n=18 wr **0.56** | n=50 wr 0.58 | **−0.09R** |
| вместе | n=44 wr 0.43 | n=136 wr 0.54 | −0.24R, p=0.079 |

Знак один и тот же на всех трёх, но на третьем эффект практически исчез.

## 11. УРОК L3 — О СОБСТВЕННОМ ПРОЦЕССЕ

Доли побед по сегментам без всякого фильтра: **46% / 57% / 58%**. Разброс
между сегментами — того же порядка, что весь эффект фильтра.

> **L3: я приписал правилу то, что может быть свойством периода.**
> Прежде чем объяснять winrate правилом, надо проверить, не объясняется ли он
> сегментом.

Это ровно тот режим отказа, о котором предупреждал пользователь, и он
сработал на мне, а не в теории.

## 12. ПОЧЕМУ Я ОСТАНОВИЛСЯ, А НЕ ДОБАВИЛ ЕЩЁ ОДНО ПРАВИЛО

Устойчивый знак на всех трёх сегментах есть у четырёх признаков:

| Признак | SEG-1 | SEG-2/3 | SEG-4 |
|---|---|---|---|
| `vol20 <= 0.4` | +0.88 | +0.49 | +0.19 |
| `span20 <= 3.0` | +0.74 | +0.21 | +0.11 |
| `net60 <= 0` | +0.72 | +0.19 | +0.09 |
| `pos60 <= 0.25` | +0.16 | +0.27 | +0.26 |

Соблазн очевиден. Но пороги выбраны **после** просмотра всех трёх сегментов,
а свободных данных больше нет — ни одного бара.

Сделал единственную честную проверку, которая ещё возможна:
**leave-one-segment-out** (`research/loop/loso.py`) — фильтр выбирается на двух
сегментах и проверяется на третьем, по кругу.

```
держим SEG-1     выбран [vol20 <= 0.4 ] → на отложенном осталось 3 сделки — мало
держим SEG-2/3   выбран [vol20 <= 0.4 ] → n=12 wr=0.67 meanR +0.385 (было -0.074, +0.459R)
держим SEG-4     выбран [span20 <= 2.5] → на отложенном осталось 8 сделок — мало
```

**Вывод сделан только в одной ротации из трёх.** Этого не хватает.
Правило в политику **не внесено**, записано как непроверенное убеждение B6.

## 13. СОСТОЯНИЕ ОБУЧЕНИЯ

| | |
|---|---|
| Живых сделок | **144** (SEG-1 19, SEG-2 25, SEG-3 40, SEG-4 60) |
| Побед | 71 (49%) |
| Итог | −1.2R, ожидание −0.008R |
| Со спредом ≈0.06R | **−0.068R** |
| Кандидатов без фильтра, 3 сегмента | 180, побед 93 (52%), p(≠50%) = 0.71 |
| Целостность трёх леджеров | OK |

**Текущая гипотеза H1 (ослаблена):** после сжатия волатильности цена чаще идёт
вниз, но не тогда, когда падение последних 20 баров уже состоялось.
Kill criteria не сработали (wr 0.49 против порога 0.35; ожидание −0.008R против
порога −0.25R), поэтому H1 жива — но `B1` переведена в `WEAKENED`.

| Убеждение | n | wr | статус |
|---|---|---|---|
| B1 сжатие → вниз | 143 | 0.49 | **WEAKENED** |
| B3 `net20 > -2` выигрывает | 141 | 0.50 | HELD (ровно безубыток) |
| B4 `net20 <= -1` проигрывает | 12 | 0.25 | HELD, но лишь на 2 сегментах из 3 |
| B5 `net20 > -1` выигрывает | 132 | 0.52 | HELD |
| B6 `vol20 <= 0.4` лучше | — | — | **UNTESTED, в политику не внесено** |

**Исправленный баг механизма.** `status_for()` считал, что любое убеждение
предсказывает выигрыш, поэтому B2 и B4 — сформулированные как «проигрывает» —
получали `REJECTED` от той самой записи, которая их подтверждает. Добавлено
поле `direction`; статусы пересчитаны.

## 14. ЧЕСТНЫЙ ИТОГ ЭТАПА

Цикл работает: гипотеза выведена из сырых данных, два урока превратились в
исполняемые правила, изменение поведения измерено (6 расхождений на истории,
11 блокировок вперёд), один урок подтвердился проспективно и затем **сломался
на четвёртом сегменте**, и это зафиксировано, а не спрятано.

Заработка нет. 144 сделки, 49% побед, −0.008R, со спредом −0.068R.
На 180 кандидатах трёх сегментов p = 0.71 — от монетки не отличить.

Данные исчерпаны. Дальнейшее развитие требует **новых баров**, а не новых
правил на старых: любое правило, которое я добавлю сейчас, будет подогнано
под всё, что у меня есть, без единого способа это проверить.

---

# ЭТАП 3 — новые данные: 15-минутный таймфрейм

## 15. ЧТО ПРИШЛО И ЧТО ИЗ ЭТОГО ВЗЯТО

Файл `OANDA_XAUUSD_15`, 20 457 баров. Проверка: шаг между барами 900 секунд —
это **15-минутный** таймфрейм, не 3-минутный, и период другой.

| Часть | Баров | Решение |
|---|---|---|
| до 2026-07-12 | **16 199** | **SEG-5** — эта ценовая история не использовалась ни в одном эксперименте ни в каком виде |
| после 2026-07-12 | 4 258 | отрезано: та же ценовая история, что 3м-сегменты, другая агрегация |

Совпадение 4 167 меток времени с уже использованным — ложное: каждый 15м бар
совпадает по метке с одним из 3м баров, но это разные объекты.

## 16. ПОПРАВКА, ОБЪЯВЛЕННАЯ ДО ПРОГОНА

Геометрия скобки на новом таймфрейме ломается:

| | стоп в медианных диапазонах бара |
|---|---|
| 3м, скобка 0.15% | 1.93 / 2.26 / 1.98 по трём сегментам |
| 15м, скобка 0.15% | **0.79** |

Стоп помещался бы **внутрь одного медианного бара**, и при пессимистичном
правиле «ничья → стоп» тест мерил бы соглашение о ничьих, а не гипотезу.

Скобка изменена **0.15% → 0.376%**, что восстанавливает те же 1.98 диапазона.
SL и TP масштабированы одинаково, отношение 1:1 сохранено, `max_bars` и
`cooldown` оставлены в барах без изменений. Правила решений не тронуты.
Объявлено и записано **до** первого решения.

## 17. РЕЗУЛЬТАТ SEG-5

| | |
|---|---|
| Сделок | **158** |
| Побед | **92 (58%)** |
| Итог | **+27.4R**, ожидание **+0.173R** |
| p (≠50%) | **0.046** |
| Издержки | спред 0.30 против стопа 17.0 пункта = 0.018R → **чистое +0.156R** |
| По четвертям | +0.11 / +0.28 / +0.21 / +0.09 — все четыре положительны |
| По месяцам | 8 из 9 не отрицательны, худший 2026-05 (−5.5R) |

**L2 подтверждён в третий раз, на новом таймфрейме и новом периоде:**
R0 заблокировал 29 сделок, из них 8 побед (28%), предотвращено −12.6R.

Впервые за весь проект эффект заметно превысил издержки.

## 18. ПОЧЕМУ Я НЕ ОБЪЯВЛЯЮ ЭТО КРАЕМ

**Поправка на множественность.** Это пятый сегмент. `0.046 × 5 = 0.23`.
После поправки результат **не значим**.

**Чувствительность к скобке.** Знак положителен на всех пяти значениях, и это
хорошо. Но объявленное 0.376% оказалось ровно на пике:

```
0.250% → +0.127R p=0.138      0.376% → +0.173R p=0.046   ← объявлено заранее
0.300% → +0.141R p=0.099      0.450% → +0.108R p=0.230
                              0.550% → +0.041R p=0.628
```

Значение выбрано по геометрическому правилу до прогона, но совпадение с пиком
я исключить не могу.

**Вклад урока меньше вклада гипотезы.** Без R0 было бы +0.142R (p=0.092);
L2 добавляет +0.031R.

## 19. РЕШАЮЩИЙ ТЕСТ: ЭТО ТАЙМФРЕЙМ ИЛИ ПЕРИОД?

Прогнал 15м на **том же отрезке цены**, что и 3м-сегменты (2026-07-12 → 09-15),
с выровненной геометрией скобки.

| Данные | Период | n | побед |
|---|---|---|---|
| 15м | 2025-11 → 2026-07 | 158 | **58%** |
| 15м | 2026-07 → 09 | 31 | **35%** |
| 3м | 2026-07 → 09 | 180 | 52% |

На одном и том же периоде 15м оказался **хуже** 3м. **Преимущества таймфрейма
нет.** Результат определяется периодом.

Проверил на баг: три разные скобки дали одинаковую сумму −9.0R, но отдельные
сделки и длительности различаются (медиана 6 / 7 / 9 / 10 баров, сделка на
баре 536 меняет знак). Совпадение сумм на n=31 — случайность, механика исправна.

> **L4: результат определяется периодом, а не таймфреймом и не правилом.**
> Четыре независимых замера: 35% / 46% / 52% / 58%.
> Будь у меня только SEG-5, я бы отчитался о работающем крае с p=0.046.

Это продолжение L3 и его усиление: на этапе 2 разброс между периодами был
сопоставим с эффектом фильтра, теперь он сопоставим со **всем** эффектом.

## 20. СОБСТВЕННАЯ ОШИБКА В ЭТОЙ ЖЕ ПАРТИИ

Я заявил, что убеждение B6 (`vol20 <= 0.4`) получило проспективное
подтверждение, и завёл политику v4 с правилом R00 на его основе.

**Это было неверно.** Контрфактуальная проверка показала, что v4 отсекла бы
**97%** сделок SEG-5 — что несовместимо с заявленным n=23. Расхождение
заставило проверить, и выяснилось:

| Сегмент | доля сделок с `vol20 <= 0.4` |
|---|---|
| SEG-1 | 3/19 = 16% |
| SEG-2/3 | 6/65 = 9% |
| SEG-4 | 8/60 = 13% |
| **SEG-5 (15м)** | **6/158 = 4%** |

`vol20` — это отношение стандартного отклонения доходностей к медианному
диапазону бара, и оно зависит от таймфрейма. **Порог 0.4 означает разное на
разных таймфреймах.** Сумма 23 — это пул; на единственном новом сегменте
наблюдений всего шесть.

Правило R00 отозвано, политика v4 удалена, B6 возвращено в `UNTESTED`.
Действующей остаётся v3. Инцидент записан в леджер.

## 21. СОСТОЯНИЕ ОБУЧЕНИЯ ПОСЛЕ ЭТАПА 3

| | |
|---|---|
| Живых сделок | **302** (SEG-1 19, SEG-2 25, SEG-3 40, SEG-4 60, SEG-5 158) |
| Побед | 163 (**54%**) |
| Итог | **+26.2R**, ожидание **+0.087R** |
| p (≠50%) | **0.186** |
| Целостность четырёх леджеров | OK |

| Убеждение | n | wr | статус |
|---|---|---|---|
| B1 сжатие → вниз | 301 | 0.538 | HELD (было WEAKENED, восстановилось на SEG-5) |
| B4 `net20 <= -1` проигрывает | 12 | 0.25 | **HELD** — подтверждено на 3 сегментах из 4 |
| B5 `net20 > -1` выигрывает | 290 | 0.552 | HELD |
| B6 `vol20 <= 0.4` лучше | 23 (пул) | — | **UNTESTED**, порог зависит от таймфрейма |

**Текущая гипотеза H1:** после сжатия волатильности цена чаще идёт вниз, но не
тогда, когда падение последних 20 баров уже состоялось (`net20 <= -1.0`).
Политика v3, три версии за весь прогон, ядро `R1` не менялось ни разу.

**Что бы решило вопрос.** Один сегмент 15м из периода, который ещё не
использовался. Всё остальное — переливание из пустого в порожнее на тех же
данных.

---

# ЭТАП 4 — другой инструмент и проверка механизма

## 22. SEG-7: NASDAQ

Файл `IG_NASDAQ_15`: 643 бара 15м, 2026-09-08 → 09-17. **Другой инструмент** —
гипотеза выведена на золоте и другого рынка не видела ни разу.

Объявлено **до** прогона:
- скобка **0.222%** по тому же геометрическому правилу (стоп = 1.98 медианных
  диапазона бара; при 0.15% вышло бы 1.34);
- **порог для вывода n ≥ 25**; при меньшем n результат записывается, но вывод
  не делается, каким бы он ни оказался.

**Результат: 4 сделки, 1 победа. Порог не достигнут — вывода нет.**

Для n ≥ 25 нужно около 2 500 баров NASDAQ 15м, то есть 5–6 недель торгов.

## 23. ГЛАВНОЕ ЗА ВЕСЬ ПРОГОН: ПРОВЕРКА МЕХАНИЗМА

643 бара мало для торгового теста, но достаточно, чтобы задать другой вопрос —
и я впервые проверил не доходность, а **саму заявленную причину**.

H1 утверждает: после сжатия цена чаще идёт вниз. Это проверяется напрямую,
без сделок, скобок и кулдаунов — доля роста за 20 баров после сжатия против
базовой доли.

| Сегмент | база | после сжатия | асимметрия |
|---|---|---|---|
| LEARN 3м (здесь выведена) | 47.8% | 38.0% | **−9.8 п.п.** |
| SEG-1 3м | 43.3% | 31.7% | **−11.6 п.п.** |
| SEG-2/3 3м | 51.8% | 46.7% | −5.1 п.п. |
| SEG-4 3м | 47.8% | 50.7% | **+3.0 п.п.** (знак обратный) |
| SEG-5 15м | 51.0% | 50.1% | **−0.9 п.п.** (исчезла) |
| SEG-7 NASDAQ | — | n=21 | мало |

Монотонное угасание с разворотом знака — классическая подпись эффекта,
который в значительной мере был свойством выборки.

**И вот решающее.** SEG-5 — тот самый сегмент, что дал +27.4R при p=0.046, —
имеет асимметрию **−0.9 п.п.**, то есть практически нулевую.

> **L5: та прибыль пришла не от заявленного механизма.**
> Правила заработали там, где причина, ради которой они написаны, не работает.
> Прибыль без работающего механизма — это не край, это необъяснённый артефакт.

## 24. ОШИБКА В МОЁМ СОБСТВЕННОМ ПРОТОКОЛЕ

Kill criteria H1 были записаны до первой сделки и звучали так:
«≥12 сделок и доля побед < 0.35», «≥20 сделок и ожидание хуже −0.25R».

Они **не сработали ни разу** — и формально были правы: 54% побед, +0.079R.

Но они все про **доходность**. Критерия, который убивает гипотезу при падении
её **заявленной причины**, я не заложил. Механизм разваливался четыре сегмента
подряд, а протокол этого не видел, потому что смотрел не туда.

Это ошибка проектирования протокола, а не данных. Будь у меня такой критерий с
самого начала, H1 умерла бы на SEG-4, а не держалась бы до NASDAQ.

**Правило на будущее:** у гипотезы должно быть два независимых kill criteria —
по результату и по механизму. Гипотеза, которую нельзя убить измерением её
собственной причины, не фальсифицируема.

## 25. СОСТОЯНИЕ ОБУЧЕНИЯ ПОСЛЕ ЭТАПА 4

| | |
|---|---|
| Живых сделок | **306** на шести сегментах, двух таймфреймах, двух инструментах |
| Побед | 164 (**54%**) |
| Итог | +24.2R, ожидание **+0.079R** |
| p (≠50%) | **0.230** |
| Целостность пяти леджеров | OK |

| Убеждение | статус |
|---|---|
| B1 сжатие → вниз | **REJECTED по механизму** |
| B4 `net20 <= -1` проигрывает | HELD на 3 сегментах из 4 |
| B5 `net20 > -1` выигрывает | HELD (0.55) |
| B6 `vol20 <= 0.4` | UNTESTED, порог зависит от таймфрейма |

**H1 переведена в MECHANISM-REJECTED.** Правила v3 больше не имеют
обоснования. Они не убыточны — но называть их работающими нельзя: причина, ради
которой они написаны, не воспроизводится.

Единственное, что пережило все шесть сегментов, — это **L2/B4**, и это не
гипотеза, а фильтр: не шортить сжатие после уже состоявшегося падения.
Он подтвердился на SEG-1, SEG-3 и SEG-5 и опроверг себя на SEG-4.

## 26. ЧЕМ ЗАКОНЧИЛСЯ ЦИКЛ

Цикл сделал ровно то, для чего строился: вывел гипотезу из сырых данных,
превратил два урока в исполняемые правила, измерил изменение поведения вместо
того, чтобы его декларировать, поймал четыре собственные ошибки (повтор
обучающей выборки под видом нового сегмента, знаковый баг в механизме
убеждений, ложное подтверждение B6, утечку горизонта в отчёте) — и в итоге
**убил собственную гипотезу измерением её причины**, а не отчаявшись от убытков.

Торгового края не найдено. 306 сделок, 54%, p = 0.230.
