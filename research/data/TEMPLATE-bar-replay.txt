# ШАБЛОН РУЧНОГО СБОРА ЧЕРЕЗ BAR REPLAY
#
# Заполняйте по одной строке на бар, сверху вниз, СТАРЫЕ БАРЫ ПЕРВЫМИ.
# Разделитель — табуляция (Tab). Порядок колонок менять можно.
# Пустая ячейка или "-" означает "значение не прочитано". Так и оставляйте:
# пустое поле честнее выдуманного, харнесс сам сообщит, чего не хватает.
#
# ПРОГРЕВ (первые ~20 баров): достаточно time, high, low, close, vwap.
#   ATR считается по high/low/предыдущему close, режим — по vwap за 20 баров.
# РАБОЧИЕ БАРЫ (с 21-го): заполняйте все колонки.
#   BIAS желательно начать заполнять на 5 баров раньше первого рабочего —
#   тогда в снимке будет поле "изменение bias за 5 баров". Без него не страшно.
#
# open и volume НЕ НУЖНЫ — харнесс их не использует. Не тратьте на них время.
#
# Сколько собрать: рабочих баров получится (всего − 20).
#   40 баров -> 20 рабочих -> 40 решений (каждый бар проверяется в обе стороны).
#   45 баров -> 25 рабочих -> 50 решений. Это разумная цель.
#
# Строки, начинающиеся с #, игнорируются.

time	high	low	close	vwap	poc	vah	val	+2	-2	bias	conf
